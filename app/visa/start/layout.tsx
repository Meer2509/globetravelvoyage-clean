import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Start Your Visa Request",
  description:
    "Submit a visa inquiry to verified experts on Globe Travel Voyage. Get guidance, document checklists, and application support.",
};

export default function VisaStartLayout({ children }: { children: React.ReactNode }) {
  return children;
}
