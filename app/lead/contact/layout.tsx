import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Contact Our Team",
  description:
    "Contact Globe Travel Voyage for partnership inquiries, medical travel, corporate bookings, and custom travel requests.",
};

export default function LeadContactLayout({ children }: { children: React.ReactNode }) {
  return children;
}
